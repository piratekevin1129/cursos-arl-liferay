function clickNextPaginator(btn){
    var paginator_parent = btn.parentNode
    var wrap = paginator_parent.getAttribute('wrap')
    var actual_page = paginator_parent.getAttribute('page')
    
    if(actual_page)
}

function clickPrevPaginator(btn){

}